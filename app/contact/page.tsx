import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Contact",
  description: "Book a discovery call to discuss your custom software needs.",
};

export { default } from "./page-client";
