import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "How We Work",
  description: "Every engagement starts with a prototype and ROI model — not a PowerPoint.",
};

export { default } from "./page-client";
